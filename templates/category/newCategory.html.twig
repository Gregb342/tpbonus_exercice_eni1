{% extends 'base.html.twig' %}

{% block main %}
 <div class="container-fluid col-5">
     <h1>Créer une catégorie | <a href="{{ path('app_categories') }}" class="btn btn-sm btn-success">Retour aux catégories</a></h1>

     <div class="container-fluid card row p-3">
         {{form_start(productForm)}}

         {{form_widget(productForm)}}

        <button class="btn btn-primary" type="submit">Enregistrer la catégorie</button>

         {{form_end(productForm)}}

     </div>
 </div>



{% endblock %}